// Write a program which define any integer and which also enters number of times the enter number to be 		printed.
let number = 7;
let times = 5; 

for (let i = 0; i < times; i++) {
    console.log(number);
}