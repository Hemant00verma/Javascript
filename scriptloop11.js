// Write a program which entre the no. of odd no. to be generated and printed and that may odd no. should be 		generated and printed.
let n=10;
let count = 0;  
let num = 1;    

while (count < n) {
    console.log(num); 
    num = num+2; 
    count++;  
}
